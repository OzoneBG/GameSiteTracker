namespace GST.Web.Infrastructure
{
    using GST.Data;
    using Microsoft.EntityFrameworkCore;

   //public class DBInitializer
   //{
   //    public static void Init()
   //    {
   //        DbContextOptions<GSTDbContext> contextOptions = new DbContextOptions<GSTDbContext>();
   //
   //        using (var context = new GSTDbContext(contextOptions))
   //        {
   //            context.Database.Migrate();
   //        }
   //    }
   //}
}
