@using GST.Web.ViewModels.PicturesViewModels;
@using GST.Web.Common
@model List<PicturesViewModel>
@{ 
    ViewData["Title"] = "Media | Pictures";
}
<h4 class="content-header">List of all pictures</h4>
<div class="container">
    @if (Model.Count == 0)
    {
        <h3>No pictures</h3>
    }
    else
    {
        @:<div class="row">
        int count = 0;
        int totalSize = Model.Count;
        foreach (var picture in Model)
        {
            <div class="col-md-4">
                <div class="container">
                    <img src="@picture.UrlToImage" width="300" height="200" data-toggle="modal" data-target="#@count" />
                    <div class="modal fade" id="@count" role="dialog" width="750">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                                    <h4 class="modal-title">@picture.Name <a href="@picture.UrlToImage">View full size</a></h4>
                                </div>
                                <div class="modal-body">
                                    <img src="@picture.UrlToImage" width="560" height="350" />
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            count++;
            totalSize--;
            if (count % 3 == 0)
            {
                if (totalSize > 0)
                {
                    @:</div><br /><div class="row">
                }
                else
                {
                    @:</div>
                }
            }
        }
    }
</div>
<nav aria-label="Page navigation">
    <ul class="pagination">
        @if (ViewBag.CurrentPage > 1)
        {
            <li class="page-item">
                <a class="page-link" asp-action="Pictures" asp-controller="Home" asp-route-p="@(ViewBag.CurrentPage-1)" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">Previous</span>
                </a>
            </li>
        }
        @{
            for (int i = 1; i <= ViewBag.TotalLinksToDisplay; i++)
            {
                if (ViewBag.CurrentPage == i)
                {
                    <li class="page-item active">@Html.ActionLink(@i.ToString(), "Pictures", "Home", new { p = @i }, new { @class = "page-link" })</li>
                }
                else
                {
                    <li class="page-item">@Html.ActionLink(@i.ToString(), "Pictures", "Home", new { p = @i }, new { @class = "page-link" })</li>
                }
            }
        }
        @if (ViewBag.CurrentPage < ViewBag.TotalLinksToDisplay)
        {
            <li class="page-item">
                <a class="page-link" asp-action="Pictures" asp-controller="Home" asp-route-p="@(ViewBag.CurrentPage+1)" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">Next</span>
                </a>
            </li>
        }
    </ul>
</nav>


<!--<div class="row">
    <div class="col-md-4">
        <div class="container">
            <img src="https://images5.alphacoders.com/643/643094.jpg" width="300" height="200" data-toggle="modal" data-target="#imageModal" />
            <div class="modal fade" id="imageModal" role="dialog" width="750">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal">&times;</button>
                            <h4 class="modal-title">An awesome picture of Geralt <a href="https://images5.alphacoders.com/643/643094.jpg">View full size</a></h4>
                        </div>
                        <div class="modal-body">
                            <img src="https://images5.alphacoders.com/643/643094.jpg" width="560" height="350" />
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>-->